# [Legacy] MySQL 联调：Flyway 迁移仅支持 PostgreSQL，此 profile 不启用 Flyway
spring:
  flyway:
    enabled: false
  datasource:
    url: jdbc:mysql://localhost:3306/furuiduo_quote?useSSL=false&serverTimezone=Asia/Shanghai&characterEncoding=utf8
    username: root
    password: 123456
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    database-platform: org.hibernate.dialect.MySQLDialect
    show-sql: false
